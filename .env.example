# Exemplo de variáveis de ambiente para o Gateway
# Copie este arquivo para `.env` e preencha as chaves reais.

# Chave do Google Gemini (opcional para desenvolvimento). Se não fornecida, o
# gateway usará um roteador rule-based de fallback.
GEMINI_API_KEY=

# Chave da API do Mercado Livre (não obrigatória para buscas públicas simples)
MERCADOLIVRE_API_KEY=

# Personal Access Token do Figma
FIGMA_API_KEY=
